[
  {
    "function_v0": {
      "doc": "Read the configured admin (the account that funded the contract).",
      "name": "admin",
      "inputs": [],
      "outputs": [
        {
          "option": {
            "value_type": "address"
          }
        }
      ]
    }
  },
  {
    "function_v0": {
      "doc": "Submit a guess. Returns `Ok(true)` if the guess matches and the\\ncaller has been rewarded, `Ok(false)` on a wrong guess.\\nReturns:\\n- `Err(InvalidGuess)` if the guess is outside `1..=5`.\\n- `Err(InsufficientRewardFunds)` if the contract cannot pay the\\nfull 10 XLM reward (no transfer, no reset).\\n- `Err(FailedToTransferReward)` if the token transfer itself fails.\\n\\nOn a correct guess the contract pays 10 XLM to the `guesser` and\\nimmediately rolls a new random number for the next round.",
      "name": "guess",
      "inputs": [
        {
          "doc": "",
          "name": "user_number",
          "type_": "u64"
        },
        {
          "doc": "",
          "name": "guesser",
          "type_": "address"
        }
      ],
      "outputs": [
        {
          "result": {
            "ok_type": "bool",
            "error_type": "error"
          }
        }
      ]
    }
  },
  {
    "function_v0": {
      "doc": "Constructor.\\n\\n`admin` funds the contract with `INITIAL_FUNDING_XLM` XLM out of its\\nown balance (the deployer should friendbot the admin first).\\n`xlm` is the address of the XLM Stellar Asset Contract on the target\\nnetwork \\xe2\\x80\\x94 the deployer supplies it explicitly so the contract stays\\nnetwork-agnostic.",
      "name": "__constructor",
      "inputs": [
        {
          "doc": "",
          "name": "admin",
          "type_": "address"
        },
        {
          "doc": "",
          "name": "xlm",
          "type_": "address"
        }
      ],
      "outputs": []
    }
  },
  {
    "udt_error_enum_v0": {
      "doc": "",
      "lib": "",
      "name": "Error",
      "cases": [
        {
          "doc": "The contract failed to transfer the 10 XLM reward to the guesser.",
          "name": "FailedToTransferReward",
          "value": 1
        },
        {
          "doc": "The contract does not currently hold enough XLM to pay the 10 XLM reward.",
          "name": "InsufficientRewardFunds",
          "value": 2
        },
        {
          "doc": "The guess must be a whole number between 1 and 5 (inclusive).",
          "name": "InvalidGuess",
          "value": 3
        }
      ]
    }
  }
]