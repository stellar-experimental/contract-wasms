[
  {
    "function_v0": {
      "doc": "Method `a` calls Contract B's method `b`, forwarding the `volta` address.\\n\\nCall chain: Volta -> A.a -> B.b(volta, value)\\nB.b requires `volta.require_auth()`, so when invoking through Volta,\\nproper `auth_entries` must be provided to authorize the sub-call.",
      "name": "a",
      "inputs": [
        {
          "doc": "",
          "name": "contract_b",
          "type_": "address"
        },
        {
          "doc": "",
          "name": "volta",
          "type_": "address"
        },
        {
          "doc": "",
          "name": "value",
          "type_": "u64"
        }
      ],
      "outputs": [
        "u64"
      ]
    }
  }
]